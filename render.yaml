services:
  - type: web
    name: shifokor-bot
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
